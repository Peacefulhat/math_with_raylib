#include"Ui/canvas.h"

int main(){
  /*
    InitWindow(WIDTH,HEIGHT,"MyWindow");
    while(!WindowShouldClose()){
    BeginDrawing();
    ClearBackground(BLACK);
    animateCircle();
    EndDrawing();
    }
    CloseWindow();
  */

animateCircleCircular(0.05f,0.04f);
return 0;
 
}
